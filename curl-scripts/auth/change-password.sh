# VARIABLE=VALUE sh curl-scripts/auth/change-password.sh

curl "https://powerful-beyond-68228.herokuapp.com" \
  --include \
  --request PATCH \
  --header "Content-Type: application/json" \
  --header "Authorization: Token token=${TOKEN}" \
  --data '{
    "passwords": {
      "old": "'"${OLD}"'",
      "new": "'"${NEW}"'"
    }
  }'

echo
